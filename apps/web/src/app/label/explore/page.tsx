export default function ExploreLabelPage() {
  return (
    <div className="p-8">
      <h1 className="text-xl font-semibold text-teal-400">Free explore labeling</h1>
      <p className="mt-2 text-sm text-slate-400">
        Map + chip viewer + bbox drawing — next iteration. Use Finder map to pick coordinates for now.
      </p>
    </div>
  );
}
